<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>FluffyFriend — Admin Dashboard</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    html, body {
      height: 100%;
      margin: 0;
      font-family: 'Segoe UI', sans-serif;
      background: #fff6fb;
      display: flex;
      flex-direction: column;
    }
    .navbar {
      background: linear-gradient(90deg,#ff8db8,#ff6fa3);
    }
    .navbar-brand {
      color:#fff; font-weight:700;
    }
    .nav-link {
      color:#fff !important;
      font-weight:500;
      border-radius:8px;
      padding:8px 14px;
      transition:.3s;
    }
    .nav-link:hover {
      background:#ff8db8;
      color:#fff !important;
    }
    main {
      flex: 1;
    }
    .admin-hero {
      background: linear-gradient(135deg,#ffeef8,#ffd5ea);
      border-radius:20px;
      box-shadow:0 25px 70px rgba(255,111,163,0.25);
      padding:32px;
      margin:28px auto;
      max-width:1100px;
      position:relative;
      overflow:hidden;
    }
    .admin-hero::after {
      content:'';
      position:absolute;
      top:-40px;
      right:-40px;
      width:160px;
      height:160px;
      background:rgba(255,255,255,0.3);
      border-radius:50%;
      filter:blur(0.5px);
    }
    .hero-avatar {
      width:72px;
      height:72px;
      border-radius:24px;
      background:#fff;
      display:flex;
      align-items:center;
      justify-content:center;
      font-size:2rem;
      color:#ff6fa3;
      box-shadow:0 10px 25px rgba(255,111,163,0.25);
    }
    .hero-stats {
      display:grid;
      grid-template-columns:repeat(auto-fit,minmax(140px,1fr));
      gap:16px;
      margin-top:24px;
    }
    .hero-stat {
      background:rgba(255,255,255,0.65);
      border-radius:16px;
      padding:14px 16px;
      backdrop-filter:blur(6px);
      box-shadow:0 8px 20px rgba(255,111,163,0.15);
    }
    .hero-stat-label {
      font-size:0.75rem;
      text-transform:uppercase;
      letter-spacing:0.08em;
      color:#b98ca3;
      margin-bottom:4px;
    }
    .hero-stat-value {
      font-size:1.5rem;
      font-weight:700;
      color:#ff3e8a;
    }
    .card-soft {
      background:#fff;
      border-radius:18px;
      border:0;
      padding:24px;
      text-align:left;
      transition: transform .25s, box-shadow .25s, background .25s;
      box-shadow:0 10px 24px rgba(0,0,0,0.06);
      cursor:pointer;
      position:relative;
      overflow:hidden;
    }
    .card-soft::after {
      content:'';
      position:absolute;
      inset:0;
      background:linear-gradient(135deg,rgba(255,111,163,0.08),rgba(255,159,211,0.08));
      opacity:0;
      transition:opacity .25s;
    }
    .card-soft:hover {
      transform: translateY(-6px);
      box-shadow:0 20px 35px rgba(255,111,163,0.25);
    }
    .card-soft:hover::after {
      opacity:1;
    }
    .card-icon {
      width:56px;
      height:56px;
      border-radius:16px;
      background:rgba(255,111,163,0.12);
      display:flex;
      align-items:center;
      justify-content:center;
      font-size:1.65rem;
      color:#ff6fa3;
      margin-bottom:14px;
      position:relative;
      z-index:1;
    }
    .card-soft h5,
    .card-soft p {
      position:relative;
      z-index:1;
    }
    .dash-action {
      opacity: 0;
      transform: translateY(14px);
      transition: opacity .45s ease, transform .45s ease;
    }
    .dash-action.reveal {
      opacity: 1;
      transform: translateY(0);
    }
    .btn-pink {
      background:#ff6fa3;
      border:none;
    }
    .btn-pink:hover {
      background:#ff8db8;
    }
    footer {
      background:linear-gradient(90deg,#ff8db8,#ff6fa3);
      color:#fff;
      padding:12px 0;
      margin-top:auto;
    }
  </style>
</head>

<body>
  <!-- Navbar -->
  <%- include('partials/admin_navbar') %>

  <main>
    <div class="container mt-3">
      <% if (success && success.length) { %>
        <div class="alert alert-success"><%= success[0] %></div>
      <% } %>
      <% if (error && error.length) { %>
        <div class="alert alert-danger"><%= error[0] %></div>
      <% } %>
    </div>

    <% const heroStats = stats || { users: 0, products: 0, orders: 0 }; %>

    <div class="admin-hero container text-center text-md-start">
      <div class="d-flex flex-column flex-md-row align-items-center gap-3 position-relative">
        <div class="hero-avatar">&#128062;</div>
        <div>
          <p class="text-uppercase small mb-1 text-muted">FluffyFriend Admin</p>
          <h2 class="fw-bold mb-1">Hello, <%= user.username %></h2>
          <p class="text-muted mb-0">You're logged in as <strong><%= user.role %></strong>. Keep an eye on your store at a glance.</p>
        </div>
      </div>

      <div class="hero-stats">
        <div class="hero-stat">
          <div class="hero-stat-label">Total Users</div>
          <div class="hero-stat-value"><%= heroStats.users || 0 %></div>
        </div>
        <div class="hero-stat">
          <div class="hero-stat-label">Products</div>
          <div class="hero-stat-value"><%= heroStats.products || 0 %></div>
        </div>
        <div class="hero-stat">
          <div class="hero-stat-label">Orders</div>
          <div class="hero-stat-value"><%= heroStats.orders || 0 %></div>
        </div>
      </div>
    </div>

    <div class="container mb-5">
      <div class="row g-4">
        <!-- Users box. -->
        <div class="col-md-4 dash-action" style="transition-delay: 80ms;">
          <a href="/admin/users" style="text-decoration:none; color:inherit;">
            <div class="card card-soft h-100">
              <div class="card-icon">👥</div>
              <h5 class="mb-2 fw-bold">Users</h5>
              <p class="text-muted small">Create, update, or remove users.</p>
            </div>
          </a>
        </div>

        <!-- Products box -->
        <div class="col-md-4 dash-action" style="transition-delay: 160ms;">
          <a href="/admin/products" style="text-decoration:none; color:inherit;">
            <div class="card card-soft h-100">
              <div class="card-icon">🛍️</div>
              <h5 class="mb-2 fw-bold">Products</h5>
              <p class="text-muted small">Add and edit products for the store.</p>
            </div>
          </a>
        </div>

        <!-- Orders box -->
        <div class="col-md-4 dash-action" style="transition-delay: 240ms;">
          <a href="/admin/orders" style="text-decoration:none; color:inherit;">
            <div class="card card-soft h-100">
              <div class="card-icon">🧾</div>
              <h5 class="mb-2 fw-bold">Orders</h5>
              <p class="text-muted small">Review customer orders and status.</p>
            </div>
          </a>
        </div>
      </div>
    </div>
  </main>

  <footer>
    <div class="container d-flex justify-content-between align-items-center">
      <small>© <%= new Date().getFullYear() %> FluffyFriend.</small>
      <small>Made with ♥ in a pink theme.</small>
    </div>
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <script>
    window.addEventListener('load', function () {
      document.querySelectorAll('.dash-action').forEach(function (card) {
        card.classList.add('reveal');
      });
    });
  </script>
</body>
</html>
