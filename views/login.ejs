<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>FluffyFriend - Login</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />

  <style>
    html, body { height: 100%; margin: 0; display: flex; flex-direction: column; }
    main { flex: 1; }
    :root{
      --pet-900:#2b0f1d; --pet-700:#ff8db8; --pet-600:#ff6fa3; --pet-400:#ffafcc; --pet-100:#ffe9f2;
    }
    body {
      background:
        radial-gradient(circle at 20% 20%, #ffe9f4 0, rgba(255,233,244,0) 35%),
        radial-gradient(circle at 80% 10%, #ffe0f0 0, rgba(255,224,240,0) 32%),
        #fdf7fb;
      font-family: 'Segoe UI', sans-serif;
    }
    .bg-pink{ background: linear-gradient(90deg, #ff8db8, #ff6fa3) !important; }
    .btn-pink{ background-color: var(--pet-600) !important; border-color: var(--pet-600) !important; color:#fff !important;}
    .btn-pink:hover{ filter: brightness(0.95); }
    .link { color: var(--pet-700); text-decoration: none; }
    .link:hover { text-decoration: underline; }
    .navbar-brand { color: #fff; font-weight: 700; font-size: 1.3rem; }
    .navbar .nav-link { color: #fff !important; font-weight: 500; border-radius: 8px; padding: 8px 14px; transition: 0.3s ease; }
    .navbar .nav-link:hover { background-color: #ff8db8; color: #fff !important; }
    .navbar .navbar-toggler { border-color: rgba(255,255,255,.6); }
    .navbar .navbar-toggler-icon { background-image: url("data:image/svg+xml,%3csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 30 30'%3e%3cpath stroke='rgba(255,255,255,.9)' stroke-linecap='round' stroke-miterlimit='10' stroke-width='2' d='M4 7h22M4 15h22M4 23h22'/%3e%3c/svg%3e"); }
    footer { background: linear-gradient(90deg, #ff8db8, #ff6fa3); color: white; padding: 12px 0; margin-top: auto; }

    .login-hero {
      position: relative;
      min-height: calc(100vh - 140px);
      display: flex;
      align-items: center;
      padding: 48px 0;
    }
    .login-blob {
      position: absolute;
      width: 320px;
      height: 320px;
      border-radius: 40% 60% 45% 55%;
      background: linear-gradient(135deg, rgba(255,141,184,0.28), rgba(255,111,163,0.18));
      filter: blur(40px);
      z-index: 0;
      top: 16%;
      right: 8%;
    }
    .login-card {
      position: relative;
      z-index: 1;
      border: none;
      border-radius: 18px;
      box-shadow: 0 16px 50px rgba(0,0,0,0.08);
      padding: 18px;
      background: #fff;
    }
    .pill-badge {
      display: inline-block;
      background: rgba(255,111,163,0.12);
      color: var(--pet-600);
      font-weight: 700;
      padding: 6px 14px;
      border-radius: 999px;
      font-size: 0.85rem;
    }
    .check-dot {
      width: 8px;
      height: 8px;
      border-radius: 50%;
      background: var(--pet-600);
      margin-right: 10px;
      margin-top: 6px;
      flex-shrink: 0;
    }
    .input-with-toggle { position: relative; }
  </style>
</head>
<body>
  <nav class="navbar navbar-expand-lg bg-pink">
    <div class="container">
      <a class="navbar-brand fw-bold" href="/">🐾 FluffyFriend</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#ffNav">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="ffNav">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item"><a class="nav-link" href="/register">Register</a></li>
        </ul>
      </div>
    </div>
  </nav>

  <main>
    <section class="login-hero">
      <div class="login-blob"></div>
      <div class="container">
        <% if (success_msg && success_msg.length) { %>
          <div class="alert alert-success mb-3"><%= success_msg %></div>
        <% } %>
        <% if (error_msg && error_msg.length) { %>
          <div class="alert alert-danger mb-3"><%= error_msg %></div>
        <% } %>

        <div class="row g-4 align-items-center">
          <div class="col-lg-6">
            <span class="pill-badge">Welcome back</span>
            <h1 class="display-6 fw-bold mt-3 mb-2" style="color: var(--pet-900);">Sign in to FluffyFriend</h1>
            <p class="text-muted mb-3">Keep your carts synced, track orders, and get quicker checkout for your pets.</p>
            <ul class="list-unstyled text-muted small mb-0">
              <li class="d-flex mb-2"><span class="check-dot"></span><span>One account across food, toys, and grooming.</span></li>
              <li class="d-flex mb-2"><span class="check-dot"></span><span>Secure login with optional 2FA.</span></li>
              <li class="d-flex"><span class="check-dot"></span><span>Personalized recommendations for your pets.</span></li>
            </ul>
          </div>

          <div class="col-lg-5 ms-lg-auto">
            <div class="card login-card">
              <div class="card-body">
                <h2 class="h4 fw-bold text-center mb-1">Welcome Back</h2>
                <p class="text-muted text-center mb-4">Sign in to continue shopping</p>
                <form method="post" action="/login">
                  <div class="mb-3">
                    <label class="form-label">Email</label>
                    <input type="email" class="form-control" name="email" placeholder="you@example.com" required />
                  </div>
                  <div class="mb-2">
                    <label class="form-label">Password</label>
                    <div class="input-group">
                      <input type="password" class="form-control" name="password" id="passwordInput" placeholder="Your password" required minlength="6" />
                      <button type="button" class="btn btn-outline-secondary" id="togglePassword" aria-label="Show password" aria-pressed="false">Show</button>
                    </div>
                  </div>
                  <small class="text-muted">Never share your password with anyone.</small>
                  <button type="submit" class="btn btn-pink w-100 mt-3">Login</button>
                </form>

                <p class="text-muted small mt-3 mb-0 text-center">
                  Don’t have an account? <a class="link" href="/register">Register</a>
                </p>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>
  </main>

  <%- include('partials/footer') %>

  <script>
    (function() {
      const toggle = document.getElementById('togglePassword');
      const input = document.getElementById('passwordInput');
      if (!toggle || !input) return;

      toggle.addEventListener('click', () => {
        const isHidden = input.type === 'password';
        input.type = isHidden ? 'text' : 'password';
        toggle.textContent = isHidden ? 'Hide' : 'Show';
        toggle.setAttribute('aria-pressed', isHidden ? 'true' : 'false');
      });
    })();
  </script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
