<!DOCTYPE html>
<!-- updated by assistant -->
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>FluffyFriend — Home</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    html, body { height: 100%; margin: 0; display: flex; flex-direction: column; }
    body { background-color: #fff6fb; font-family: 'Segoe UI', sans-serif; }
    main { flex: 1; }

    /* Navbar */
    .navbar {
      background: linear-gradient(90deg, #ff8db8, #ff6fa3);
    }
    .navbar-brand {
      color: white;
      font-weight: 700;
      font-size: 1.3rem;
    }
    .navbar .nav-link {
      color: white !important;
      font-weight: 500;
      border-radius: 8px;
      padding: 8px 14px;
      transition: 0.3s ease;
    }
    .navbar .nav-link:hover {
      background-color: #ff8db8;
      color: #fff !important;
    }
    .navbar .navbar-toggler {
      border-color: rgba(255,255,255,.6);
    }
    .navbar .navbar-toggler-icon {
      background-image: url("data:image/svg+xml,%3csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 30 30'%3e%3cpath stroke='rgba(255,255,255,.9)' stroke-linecap='round' stroke-miterlimit='10' stroke-width='2' d='M4 7h22M4 15h22M4 23h22'/%3e%3c/svg%3e");
    }

    /* Welcome section */
    .welcome-card {
      max-width: 600px;
      margin: 100px auto 60px;
      background: white;
      border-radius: 16px;
      padding: 40px;
      text-align: center;
      box-shadow: 0 3px 12px rgba(0,0,0,0.1);
    }

    .btn-pink {
      background-color: #ff6fa3;
      border: none;
    }
    .btn-pink:hover {
      background-color: #ff8db8;
    }

    /* Popular products */
    .popular-products {
      padding: 40px 0 80px;
      text-align: center;
    }
    .product-card {
      background: white;
      border-radius: 14px;
      box-shadow: 0 3px 10px rgba(0,0,0,0.08);
      padding: 20px;
      transition: transform 0.2s;
    }
    .product-card:hover {
      transform: translateY(-5px);
    }
    .product-img {
      width: 100%;
      height: 180px;
      object-fit: cover;
      border-radius: 10px;
      margin-bottom: 15px;
    }

    /* Floating contact button */
    .fab-contact {
      position: fixed;
      right: 24px;
      bottom: 24px;
      background: linear-gradient(135deg, #ff8db8, #ff6fa3);
      color: #fff;
      padding: 14px 18px;
      border-radius: 999px;
      box-shadow: 0 12px 30px rgba(255,111,163,0.35);
      font-weight: 700;
      display: inline-flex;
      align-items: center;
      gap: 8px;
      text-decoration: none;
      transition: transform 0.15s ease, box-shadow 0.15s ease;
      z-index: 30;
    }
    .fab-contact:hover {
      transform: translateY(-4px);
      box-shadow: 0 16px 38px rgba(255,111,163,0.45);
      color: #fff;
    }
    .fab-contact svg {
      width: 18px;
      height: 18px;
    }
    .logo-paw {
      font-size: 1.3rem;
      margin-left: 6px;
    }

  </style>
</head>

<body>
  <!-- Navbar. -->
  <nav class="navbar navbar-expand-lg">
    <div class="container">
      <a class="navbar-brand" href="/">🐾 FluffyFriend</a>

      <!-- Mobile toggler -->
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#ffNav">
        <span class="navbar-toggler-icon"></span>
      </button>

      <!-- Navbar items -->
      <div class="collapse navbar-collapse" id="ffNav">
        <ul class="navbar-nav ms-auto">
          <% if (user) { %>
            <li class="nav-item"><a class="nav-link" href="/UserProducts">View All Products</a></li>
            <li class="nav-item"><a class="nav-link" href="/cart">Go To Cart</a></li>
            <% if (user.role === 'user') { %>
              <li class="nav-item"><a class="nav-link" href="/orders">My Orders</a></li>
            <% } %>
            <li class="nav-item"><a class="nav-link" href="/logout">Logout</a></li>
          <% } else { %>
            <li class="nav-item"><a class="nav-link" href="/login">Login</a></li>
            <li class="nav-item"><a class="nav-link" href="/register">Register</a></li>
          <% } %>
        </ul>
      </div>
    </div>
  </nav>

  <main>

  <!-- Carousel -->
  <%- include('partials/carousel') %>

  <!-- Welcome -->
  <div class="welcome-card">
    <% if (user) { %>
      <h2>Welcome back <%= user.username %> to FluffyFriend! <span class="logo-paw">🐾</span></h2>
      <p>Your one-stop pet shop for all furry friends.</p>
      <a href="/UserProducts" class="btn btn-pink text-white px-4">View All Products</a>
    <% } else { %>
      <h2>Welcome to FluffyFriend! <span class="logo-paw">🐾</span></h2>
      <p>Your one-stop pet shop for all furry friends.</p>
      <a href="/login" class="btn btn-pink text-white me-2 px-4">Login</a>
      <a href="/register" class="btn btn-outline-pink px-4" style="border:1px solid #ff6fa3; color:#ff6fa3;">Register</a>
    <% } %>
  </div>
  <!-- Popular Products -->
  <div class="container popular-products">
    <div class="d-flex justify-content-between align-items-center mb-4">
      <h4 class="mb-0 fw-bold">Popular Products 🐕</h4>
      <a href="/UserProducts" class="btn btn-pink text-white px-4">View All Products</a>
    </div>

    <div class="row g-4">
    <div class="col-md-4">
      <div class="product-card">
        <img src="/images/products/biooig.jpg" class="product-img" alt="Dog Food">
        <h5>Premium Dog Food</h5>
        <p class="text-muted small mb-0">Healthy and tasty meals for your pet.</p>

        <form action="/add-to-cart/1" method="POST">
          <input type="hidden" name="quantity" value="1">
          <button type="submit" class="btn mt-3" style="background-color:#ff6fa3; color:white;">Add to Cart</button>
        </form>

      </div>
    </div>

    <div class="col-md-4">
      <div class="product-card">
        <img src="/images/toys.jpg" class="product-img" alt="Cat Toy">
        <h5>Interactive Cat Toy</h5>
        <p class="text-muted small mb-0">Keeps your cat active and happy.</p>

        <form action="/add-to-cart/2" method="POST">
          <input type="hidden" name="quantity" value="1">
          <button type="submit" class="btn mt-3" style="background-color:#ff659d; color:white;">Add to Cart</button>
        </form>

      </div>
    </div>

    <div class="col-md-4">
      <div class="product-card">
        <img src="/images/grooming.jpg" class="product-img" alt="Pet Accessories">
        <h5>Pet Accessories</h5>
        <p class="text-muted small mb-0">Comfortable cages and play tools.</p>

        <form action="/add-to-cart/3" method="POST">
          <input type="hidden" name="quantity" value="1">
          <button type="submit" class="btn mt-3" style="background-color:#ff6fa3; color:white;">Add to Cart</button>
        </form>

      </div>
    </div>
  </div>

  </div>
  </main>

  <!-- Footer -->
  <%- include('partials/footer') %>

  <!-- Floating Contact button -->
  <a class="fab-contact" href="/contact" aria-label="Contact Us">
    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
      <path d="M21 10c0 6-9 11-9 11s-9-5-9-11a9 9 0 1 1 18 0Z"/>
      <circle cx="12" cy="10" r="3"/>
    </svg>
    Contact Us
  </a>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

