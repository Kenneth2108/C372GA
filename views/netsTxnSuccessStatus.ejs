<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Transaction Successful</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    :root { color-scheme: light; }

    body {
      background: radial-gradient(circle at top, #ffe8f3 0%, #fff6fb 45%, #ffeef6 100%);
      font-family: "Segoe UI", sans-serif;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }

    .ff-main {
      flex: 1 0 auto;
      padding: 60px 0 80px;
    }

    .status-card {
      max-width: 680px;
      margin: 0 auto;
      background: #fff;
      border-radius: 24px;
      padding: 34px 28px;
      box-shadow: 0 25px 60px rgba(255, 111, 163, 0.18);
      border: 1px solid #ffe1ee;
      text-align: center;
    }

    .eyebrow-label {
      letter-spacing: 0.2em;
      text-transform: uppercase;
      font-size: 0.85rem;
      color: #ff6fa3;
      font-weight: 700;
      margin-bottom: 0.6rem;
      display: inline-block;
    }

    .status-title {
      font-weight: 800;
      color: #312631;
      margin-bottom: 0.35rem;
    }

    .status-message {
      color: #6b5566;
      margin-bottom: 1.25rem;
    }

    .icon-wrap {
      width: 110px;
      height: 110px;
      border-radius: 999px;
      margin: 18px auto 16px;
      display: grid;
      place-items: center;
      background: #fff1f7;
      border: 1px solid #ffd7e8;
    }

    .icon-wrap img {
      width: 56px;
      height: 56px;
      object-fit: contain;
    }

    .success-pill {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: 8px;
      background: #eafff4;
      color: #087443;
      border: 1px solid #b7f0d2;
      padding: 8px 14px;
      border-radius: 999px;
      font-weight: 700;
      letter-spacing: 0.06em;
      text-transform: uppercase;
      font-size: 0.78rem;
      margin-bottom: 18px;
    }

    .details-box {
      text-align: left;
      background: #fff7fb;
      border: 1px solid #ffd7e8;
      border-radius: 18px;
      padding: 16px 16px;
      margin: 0 auto 18px;
      max-width: 520px;
    }

    .details-box .label {
      color: #6b5566;
      font-size: 0.92rem;
      margin: 0;
    }

    .details-box .value {
      color: #312631;
      font-weight: 700;
      margin: 0.25rem 0 0;
      word-break: break-word;
    }

    .btn-outline-pink {
      border: 1px solid #ff6fa3;
      color: #ff6fa3;
    }
    .btn-outline-pink:hover {
      background-color: #ff6fa3;
      color: #fff;
    }

    .btn-pink {
      background: #ff6fa3;
      border: 1px solid #ff6fa3;
      color: #fff;
    }
    .btn-pink:hover {
      background: #ff3d72;
      border-color: #ff3d72;
      color: #fff;
    }

    .tiny-note {
      color: #8b7283;
      font-size: 0.92rem;
      margin-top: 10px;
    }
  </style>
</head>

<body>
  <%- include('partials/user_navbar') %>

  <main class="ff-main">
    <div class="container">
      <div class="status-card">
        <span class="eyebrow-label">Payment status</span>
        <h1 class="status-title">Payment Successful</h1>
        <p class="status-message">Thank you! Your NETS payment has been completed.</p>

        <div class="icon-wrap" aria-hidden="true">
          <img src="/images/greenTick.png" alt="Transaction Successful">
        </div>

        <div class="success-pill">NETS QR • Paid</div>

        <div class="details-box">
          <p class="label mb-1">Message</p>
          <p class="value"><%= message %></p>
        </div>

        <div class="d-grid gap-2 d-sm-flex justify-content-sm-center mt-3">
          <a href="/orders" class="btn btn-pink px-4">View Order</a>
          <a href="/" class="btn btn-outline-pink px-4">Back to Home</a>
        </div>

        <p class="tiny-note">
          You can view your order details and receipt from the orders page.
        </p>
      </div>
    </div>
  </main>

  <%- include('partials/footer') %>
</body>
</html>

